import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      // main: '#ba77ff',
      main: "#6002ee",
      light: '#6002ee',
      dark: '#6002ee',
    },
    secondary: {
      main: '#11cb5f',
    },
    textColor: {
      main: '#ba77ff',
    },
  },
});

export default theme;
