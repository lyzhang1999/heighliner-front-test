import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ba77ff',
      // light: '#6002ee',
      // dark: '#6002ee',
    },
    secondary: {
      main: '#11cb5f',
    },
  },
});

export default theme;
